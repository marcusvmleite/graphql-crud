type Person {
    id: ID!
    name: String!
    email: String
    age: Int
    active: Boolean!
}

extend type Query {
    person(id: Int): Person!
    people: [Person]
}

extend type Mutation {
    savePerson(id: ID, name: String, email: String): Person
}